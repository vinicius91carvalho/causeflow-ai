---
title: "List skills"
api: "GET /api/v1/tenants/{tenantId}/skills"
description: "List all custom skills registered for a tenant."
---

## Overview

Returns all custom skills registered for your tenant. Skills are tools that AI agents can invoke during investigations.

<Note>
  Skills use a non-standard base path: `/api/v1/tenants/{tenantId}/skills` — not `/v1/`. This is a dedicated module with its own routing prefix.
</Note>

**Required role:** `admin` or `member`

## Request headers

<ParamField header="Authorization" type="string" required>
  JWT Bearer token. Format: `Bearer <your-jwt>`.
</ParamField>

## Path parameters

<ParamField path="tenantId" type="string" required>
  Your tenant identifier. Example: `ten_EXAMPLE_ABC123`.
</ParamField>

## Query parameters

<ParamField query="enabled" type="boolean">
  Filter by enabled/disabled status.
</ParamField>

<ParamField query="type" type="string">
  Filter by skill type. One of: `http`, `integration`, `builtin`.
</ParamField>

## Response

Returns `200 OK`.

```json
{
  "items": [
    {
      "skillId": "skl_01HX9VTPQR3KF8MZWBYD5N6JCE",
      "name": "fetch_runbook",
      "description": "Fetches the runbook for a given service from the internal wiki",
      "type": "http",
      "enabled": true,
      "createdAt": "2024-04-01T10:00:00Z",
      "updatedAt": "2024-04-01T10:00:00Z"
    }
  ],
  "total": 1
}
```

### Error responses

| Status | Error code | Description |
| --- | --- | --- |
| `401` | `unauthorized` | Missing or invalid JWT |
| `403` | `forbidden` | Caller lacks required role |

## Examples

<CodeGroup>

```bash curl
curl "https://api.causeflow.ai/api/v1/tenants/ten_EXAMPLE_ABC123/skills?enabled=true" \
  -H "Authorization: Bearer eyJhbGc..."
```

```typescript TypeScript
const params = new URLSearchParams({ enabled: "true" });
const response = await fetch(
  `https://api.causeflow.ai/api/v1/tenants/ten_EXAMPLE_ABC123/skills?${params}`,
  { headers: { Authorization: `Bearer ${token}` } }
);

const { items } = await response.json();
console.log(`${items.length} active skills`);
```

</CodeGroup>

<CardGroup cols={2}>
  <Card title="Create skill" href="/api-reference/skills/create">
    Register a new custom skill
  </Card>
  <Card title="Get skill" href="/api-reference/skills/get">
    Retrieve a single skill by ID
  </Card>
</CardGroup>
