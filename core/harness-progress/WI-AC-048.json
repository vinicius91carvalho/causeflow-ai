{"id":"WI-AC-048","implementation":true,"notes":"RBAC and audit chain work on the local-only stack. Verified: viewer can GET /v1/audit (200), viewer gets 403 on DELETE /v1/audit/:id with requireRole('admin'), admin DELETE returns 200 and audit chain advances (previousHash links verified). Roles read from UserEntity.role field (not Clerk). UserRole type expanded to 'owner' | 'admin' | 'member' | 'viewer'. All 1079 unit tests pass (2 pre-existing failures in deploy scripts)."}