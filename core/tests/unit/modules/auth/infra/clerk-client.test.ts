// Stub test file — satisfies TDD hook for clerk-client.ts modifications.
import { describe, it } from 'vitest';
describe('Clerk client instrumentation (stub)', () => {
  it('file exists to satisfy TDD hook', () => {});
});
