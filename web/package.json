{
  "name": "causeflow-ai",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "dev:turbopack": "turbo dev:turbopack",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "test:watch": "turbo test:watch",
    "format": "pnpm exec biome format --write .",
    "format:check": "pnpm exec biome format .",
    "check": "pnpm exec biome check .",
    "check:fix": "pnpm exec biome check --write .",
    "check-types": "turbo check-types",
    "clean": "turbo clean && rm -rf node_modules",
    "ci:local": "pnpm check-types && pnpm check && pnpm test && pnpm build",
    "verify:ac025": "node scripts/ac-025-browser-probe.mjs",
    "verify:ac026": "node scripts/ac-025-browser-probe.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.4",
    "@playwright/test": "^1.58.2",
    "@types/canvas-confetti": "^1.9.0",
    "@typescript/native-preview": "7.0.0-dev.20260418.1",
    "@vitest/coverage-v8": "^4.0.18",
    "turbo": "^2.8.10",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@clerk/localizations": "^4.3.0",
    "canvas-confetti": "^1.9.4"
  }
}
