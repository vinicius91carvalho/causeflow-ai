# QA Journal: WI-AC-043 (Verification protocol for dashboard changes documented in CLAUDE.md)

## Date: 2026-07-09

## Result: PASS - implementation=true, qa=true

### Independent Verification

1. **`apps/dashboard/CLAUDE.md` documents the four-step verification protocol** - VERIFIED
   - Section "Verification Protocol for Dashboard Changes" documents:
     1. **Local sanity**: `pnpm --filter dashboard dev` (port 3001), blank `CORE_API_URL` for mock client, Playwright codegen or scripted specs, screenshots into `.artifacts/playwright/screenshots/YYYY-MM-DD_HHmm/`, verify both browser and dev-server console are clean
     2. **Staging verification**: Sign in via `web/apps/dashboard/.env.staging` with `STAGING_TEST_USER` / `STAGING_TEST_PASSWORD` on `https://dashboard-staging.causeflow.ai`, `aws logs tail /ecs/causeflow-staging-api --follow` in parallel, `CORE_API_URL=https://api-staging.causeflow.ai` with valid Clerk staging keys for real Core responses
     3. **E2E coverage**: Every PR that touches dashboard UI or APIs ships a Playwright spec under `web/tests/e2e/dashboard/`, gated by `E2E_TARGET=staging`
     4. **Manual user steps**: Agent pauses for user confirmation, surfaces exact steps in PR description (Sentry Internal Integration, GitHub OAuth app, Slack workspace approval)
   - "Reference URLs" section lists: staging dashboard, staging core API, staging core logs command, staging login creds path

2. **`apps/dashboard/.env.staging` exists with correct structure** - VERIFIED
   - File at `apps/dashboard/.env.staging` contains `STAGING_TEST_USER=placeholder@causeflow.ai` and `STAGING_TEST_PASSWORD=placeholder-password`
   - Comment header explicitly states: "It is NOT loaded by the runtime — do NOT cp this over .env.local"

3. **`.gitignore` does NOT exclude `.env.staging`; `kill_command` kills both Next.js and Playwright** - VERIFIED
   - Root `.gitignore` has `!apps/dashboard/.env.staging` — file is intentionally trackable (Playwright reads it)
   - `CLAUDE.md` (line 226) declares `kill_command: "pkill -f 'next-server|next start|next dev' 2>/dev/null; pkill -f playwright 2>/dev/null"`
   - `CLAUDE.md` (line 278) documents rule: "Kill before starting - Before any test or server start, kill existing Next.js/Playwright processes"

### Verdict
All AC-043 steps pass independently. The verification protocol for dashboard changes is fully documented in `apps/dashboard/CLAUDE.md` with exact URLs, AWS log group path, staging credential file path, E2E requirements, and manual step instructions. No code changes required.
