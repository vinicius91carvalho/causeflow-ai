# QA Journal: WI-AC-004 (Biome 2.4.4 lint+format)

## Date: 2026-07-08

## Result: DEFECT FOUND

### Checks Passed
1. `biome.json` exists at repo root referencing Biome 2.4.4 schema - PASS
2. `@biomejs/biome@^2.4.4` in package.json devDependencies, installed version 2.4.4 - PASS
3. No `.eslintrc`, `.prettierrc`, or `eslint.config.*` files exist outside `node_modules/` - PASS
4. Malformed import (unsorted imports) triggers `assist/source/organizeImports` violation with exit 1 - PASS
5. `pnpm exec biome check --write .` auto-fixes the violation and exits 0 - PASS

### Checks Failed
1. `pnpm exec biome check .` exits 1 (not 0) against the committed tree - FAIL
   - Root cause: `feature_list.json` has multi-line single-element arrays (e.g. `"acceptance_checks": [\n  "AC-001"\n]`)
   - Biome's JSON formatter requires inline single-element arrays (`"acceptance_checks": ["AC-001"]`)
   - This produces 1 format error + 76 lint warnings (all `style/noNonNullAssertion` etc. at `warn` level)
   - After `pnpm exec biome check --write .`, JSON is reformatted and `biome check .` exits 0

### Summary
The Biome tooling is correctly installed, configured, and functional.
However, the committed tree has a pre-existing JSON formatting issue in `feature_list.json`
that prevents `pnpm exec biome check .` from exiting 0 as required by AC-004.
