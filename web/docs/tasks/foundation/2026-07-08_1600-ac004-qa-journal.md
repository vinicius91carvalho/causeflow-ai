# QA Journal: WI-AC-004 (Biome 2.4.4 lint+format)

## Date: 2026-07-08

## Result: PASS - QA=true

### Checks Performed

1. **`biome.json` exists at repo root with schema v2.4.4** - PASS
   - `$schema` is `https://biomejs.dev/schemas/2.4.4/schema.json`
   - Config includes linter (recommended + custom rules), formatter (space, 2 indent, 100 width), and `assist.actions.source.organizeImports: "on"`

2. **`@biomejs/biome@^2.4.4` in devDependencies, installed version 2.4.4** - PASS
   - root `package.json` lists `"@biomejs/biome": "^2.4.4"` in devDependencies
   - Installed version confirmed: `2.4.4`

3. **No ESLint or Prettier configs exist anywhere in repo** - PASS
   - `find . -path ./node_modules -prune -o \( -name '.eslintrc*' -o -name '.prettierrc*' -o -name 'eslint.config.*' -o -name 'prettier.config.*' \) -print` returns no results

4. **`pnpm exec biome check .` exits 0 on clean tree** - PASS
   - Exit code: 0
   - 76 warnings emitted (all `warn`-level rules: `noNonNullAssertion`, `noExplicitAny`, `noArrayIndexKey`, `useExhaustiveDependencies`)
   - No errors found

5. **Malformed import causes non-zero exit with BIOME-### violation** - PASS
   - Created test file with split same-module imports (`import { useState } from 'react'` + `import { useCallback } from 'react'`)
   - `pnpm exec biome check` exits 1 with `assist/source/organizeImports` violation ("The imports and exports are not sorted")

6. **`pnpm exec biome check --write .` auto-fixes violation and exits 0** - PASS
   - After `--write`, the two `react` imports are merged into one: `import { useCallback, useState } from 'react'`
   - Re-running the check exits 0

### Note
- Previous QA journal incorrectly claimed a defect (that `pnpm exec biome check .` exits 1). That finding was erroneous — the command exits 0 against the current tree.
