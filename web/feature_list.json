[
  {
    "id": "WI-AC-001",
    "acceptance_check_id": "AC-001",
    "acceptance_checks": [
      "AC-001"
    ],
    "context": "foundation",
    "category": "foundation",
    "title": "pnpm install bootstraps the pnpm/Turborepo workspace",
    "description": "From a clean checkout, `pnpm install` (with the pnpm version pinned by `packageManager: pnpm@10.33.0` in `package.json`) bootstraps all 7 workspace members and exits 0. `node_modules/` exists at the root and inside every workspace member listed in `pnpm-workspace.yaml` (apps/website, apps/dashboard, packages/shared, packages/ui, packages/analytics, packages/auth, packages/forms). The .npmrc pins `node-linker=hoisted`, `package-import-method=copy`, `auto-install-peers=true`, and `strict-peer-dependencies=false`. Re-running `pnpm install --frozen-lockfile` exits 0 against the committed pnpm-lock.yaml.",
    "steps": [
      "Step 1: Run `pnpm install --frozen-lockfile` and confirm exit code 0 with no peer-dep failures.",
      "Step 2: Verify each workspace member in pnpm-workspace.yaml has a populated node_modules/.",
      "Step 3: Verify .npmrc pins node-linker=hoisted, package-import-method=copy, auto-install-peers=true, strict-peer-dependencies=false."
    ],
    "depends_on": [],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 1,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-008",
    "acceptance_check_id": "AC-008",
    "acceptance_checks": [
      "AC-008"
    ],
    "context": "foundation",
    "category": "functional",
    "title": "Bounded-context re-export pattern: src/app/ holds thin re-exports only",
    "description": "Every page under `apps/website/src/app/[locale]/*/page.tsx` and `apps/dashboard/src/app/[locale]/*/page.tsx` is a 1-3 line thin re-export that delegates to a context's `presentation/pages/<name>-page.tsx`. Every API route under `apps/dashboard/src/app/api/*/route.ts` is a thin re-export that delegates to a context's `api/<name>-handler.ts`. Cross-context imports use direct deep paths — context `index.ts` barrel files do not exist in either app. `next.config.mjs` for both apps configures `optimizePackageImports` for Clerk, lucide-react, and every internal `@causeflow/*` package.",
    "steps": [
      "Step 1: Pick 3 random `apps/website/src/app/[locale]/**/page.tsx` files and confirm each is a single-line re-export.",
      "Step 2: Pick 3 random `apps/dashboard/src/app/api/**/route.ts` files and confirm each is a single-line re-export.",
      "Step 3: Confirm no `index.ts` barrel file exists under either `apps/website/src/contexts/` or `apps/dashboard/src/contexts/`."
    ],
    "depends_on": [],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 1,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-028",
    "acceptance_check_id": "AC-028",
    "acceptance_checks": [
      "AC-028"
    ],
    "context": "dashboard",
    "category": "functional",
    "title": "9 bounded contexts (investigation, approvals, audit, identity, onboarding, team, integrations, billing, settings) + shared with DDD layers",
    "description": "The dashboard's `src/contexts/` directory contains 10 bounded contexts: investigation, approvals, audit, identity, onboarding, team, integrations, billing, settings, shared. Each context has a DDD layout with only the layers it needs (domain, application, infrastructure, presentation, api). The website's `src/contexts/` directory contains 3 contexts: marketing, legal, shell. `index.ts` barrel files do not exist in any context in either app — all imports use direct deep paths. Per-context i18n files live at `infrastructure/i18n/{en,pt-br}.json` and are deep-merged by `apps/{website,dashboard}/src/lib/i18n/compose.ts`.",
    "steps": [
      "Step 1: `ls apps/dashboard/src/contexts/` shows investigation, approvals, audit, identity, onboarding, team, integrations, billing, settings, shared (10 contexts).",
      "Step 2: `ls apps/website/src/contexts/` shows marketing, legal, shell (3 contexts).",
      "Step 3: Confirm no `index.ts` exists in any of those context directories."
    ],
    "depends_on": [],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 1,
    "status": "qa_passed",
    "implemented_at": "2026-07-07T23:20:00Z",
    "implementation_notes": "Verify-first zero-diff checkpoint: structure verified at real boundaries (dashboard /api/health HTTP 200 exercising shared/api re-export; dashboard compose.test.ts deep-merges all 10 context i18n files; website / HTTP 200 renders i18n via 3-context compose). Dashboard=10 contexts, website=3 contexts, no index.ts barrels, per-context i18n at infrastructure/i18n/{en,pt-br}.json deep-merged by lib/i18n/compose.ts in both apps.",
    "qa_at": "2026-07-07T23:25:00Z",
    "qa_notes": "Independent QA pass: dashboard src/contexts/ = exactly 10 dirs (approvals, audit, billing, identity, integrations, investigation, onboarding, settings, shared, team). website src/contexts/ = exactly 3 dirs (legal, marketing, shell). find apps/{dashboard,website}/src/contexts -name index.ts returns no barrel files. Each context uses only needed DDD layers (domain/application/infrastructure/presentation/api); shared also has a documented lib/ utility dir. Per-context i18n en.json+pt-br.json exist for all 10 dashboard + 3 website contexts. Both apps/src/lib/i18n/compose.ts deep-merge via @causeflow/shared/domain/utils/deep-merge. All AC-028 steps pass.",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-033",
    "acceptance_check_id": "AC-033",
    "acceptance_checks": [
      "AC-033"
    ],
    "context": "foundation",
    "category": "ops",
    "title": "SST v3: each app's sst.config.ts provisions CloudFront + WAF + Lambda + Route 53 + ACM",
    "description": "`apps/website/sst.config.ts` and `apps/dashboard/sst.config.ts` each provision: a CloudFront distribution, a WAF WebACL (must live in `us-east-1` for CloudFront association — a separate `us-east-1` provider is created in each config), the Next.js compute (S3 + CloudFront + Lambda@Edge via OpenNext for the website; Lambda@Edge for the dashboard), Route 53 records, and an ACM certificate. The dashboard's config also provisions CloudWatch alarms for Lambda error rate and 5xx rate in production only. Domains: causeflow.ai / staging.causeflow.ai (website), dashboard.causeflow.ai / dashboard-staging.causeflow.ai (dashboard). The hosted zone is `Z01593322DGY9I94W9S7C`.",
    "steps": [
      "Step 1: Confirm `apps/website/sst.config.ts` and `apps/dashboard/sst.config.ts` each declare a `us-east-1` provider for the WAF WebACL.",
      "Step 2: Confirm both configs use `sst.aws.Nextjs` for the website and dashboard deployments.",
      "Step 3: Confirm the dashboard config provisions CloudWatch alarms and the website config does not."
    ],
    "depends_on": [],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 1,
    "status": "verify-first",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-036",
    "acceptance_check_id": "AC-036",
    "acceptance_checks": [
      "AC-036"
    ],
    "context": "foundation",
    "category": "functional",
    "title": "Auth.js v5 + Cognito: packages/auth/ retained for reference; only UserRole type re-export is consumed by the dashboard",
    "description": "`packages/auth/` retains the legacy Auth.js v5 (`next-auth@5.0.0-beta.30`) + `@auth/core` + `@aws-sdk/client-cognito-identity-provider` wiring at `packages/auth/src/infrastructure/auth-config.ts` for historical reference. The dashboard does not import the runtime exports from this package at runtime; the only consumer is the `UserRole` type re-export (`@causeflow/auth/types`) used by `apps/dashboard/src/contexts/identity/domain/rbac/__tests__/role-guard.test.ts`. The dev credentials provider auto-enables in development only; the production build strips it.",
    "steps": [
      "Step 1: Confirm `packages/auth/package.json` and `packages/auth/src/infrastructure/auth-config.ts` exist.",
      "Step 2: `grep -r \"from '@causeflow/auth\" apps/dashboard/src` returns only the `__tests__/role-guard.test.ts` import of the `UserRole` type.",
      "Step 3: Confirm `apps/dashboard/next.config.mjs` lists `@aws-sdk/client-cognito-identity-provider` in `serverExternalPackages` so the AWS SDK stays server-side."
    ],
    "depends_on": [],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-037",
    "acceptance_check_id": "AC-037",
    "acceptance_checks": [
      "AC-037"
    ],
    "context": "dashboard",
    "category": "functional",
    "title": "AWS SDK v3 limited to tenant-provisioning-fallback.ts and delete-user.ts CLI",
    "description": "The dashboard's runtime never holds AWS credentials. `@aws-sdk/client-dynamodb` and `@aws-sdk/lib-dynamodb` are imported by exactly two paths: (1) `apps/dashboard/src/contexts/identity/infrastructure/tenant-provisioning-fallback.ts`, which writes a `Tenant` and `BillingAccount` directly to the Core API's DynamoDB table when the Core's `POST /v1/tenants` returns 403 — a documented temporary workaround; (2) `apps/dashboard/scripts/delete-user.ts`, which scans and batch-deletes every item in the Core's DynamoDB table scoped to a tenantId, and also cancels the corresponding Stripe customer. The provisioning fallback is called from `complete-profile-handler.ts` on a 403 from Core; the CLI script is gated by an explicit `--yes` flag. `AWS_REGION` and AWS credentials are read from the local shell env (or IAM role in prod) and are not part of the dashboard's runtime env.",
    "steps": [
      "Step 1: `grep -r \"@aws-sdk\" apps/dashboard/src` returns only the tenant-provisioning-fallback import.",
      "Step 2: `grep -r \"@aws-sdk\" apps/dashboard/scripts` returns the delete-user import.",
      "Step 3: Confirm `apps/dashboard/.env.example` does not list `AWS_ACCESS_KEY_ID` or `AWS_SECRET_ACCESS_KEY` (these come from the shell env or SSO, not the dashboard's runtime env)."
    ],
    "depends_on": [],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 1,
    "status": "implemented",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-038",
    "acceptance_check_id": "AC-038",
    "acceptance_checks": [
      "AC-038"
    ],
    "context": "foundation",
    "category": "functional",
    "title": "Loops.so declared as planned integration; CSP allow-list but no current consumer in this codebase",
    "description": "Loops.so is declared as a planned email-marketing integration in docs and the `apps/website/.env.example` (LOOPS_API_KEY). There is no `apps/website/src/app/api/notify` route in the current codebase — `apps/website/src/app` has no `/api` directory. The only runtime reference to Loops is the `Content-Security-Policy` `connect-src` allow-list at `apps/website/next.config.mjs` (https://app.loops.so). The integration has no current consumer; it is a planned integration only.",
    "steps": [
      "Step 1: Confirm `apps/website/src/app` has no `/api` subdirectory.",
      "Step 2: Confirm `apps/website/next.config.mjs` lists `https://app.loops.so` in CSP connect-src.",
      "Step 3: Confirm `LOOPS_API_KEY` is listed in `apps/website/.env.example` but no code in `apps/website/src/` imports from any loops-related module."
    ],
    "depends_on": [],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 1,
    "status": "implemented",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-002",
    "acceptance_check_id": "AC-002",
    "acceptance_checks": [
      "AC-002"
    ],
    "context": "foundation",
    "category": "foundation",
    "title": "pnpm turbo dev brings up website on $PORT (default 3000) and dashboard on $PORT+1 (3001) in parallel",
    "description": "After `pnpm install`, `pnpm turbo dev` starts both apps in parallel with content-hash caching. The website answers on the port passed in $PORT (default 3000) and the dashboard on $PORT+1 (default 3001). `curl -s -o /dev/null -w '%{http_code}' http://localhost:${PORT}` returns 200 for the website homepage and `curl -s -o /dev/null -w '%{http_code}' http://localhost:$((PORT+1))` returns 200 for the dashboard landing page. The dev process stays in the foreground until killed.",
    "steps": [
      "Step 1: Run `PORT=3000 pnpm turbo dev` and tail `dev.log`.",
      "Step 2: Poll `curl http://localhost:3000/` until it returns 200; then poll `curl http://localhost:3001/` until 200.",
      "Step 3: Kill the dev process and confirm the script reports \"Ready\" once for each port."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 1,
    "attempts": 1,
    "status": "qa-pass",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-003",
    "acceptance_check_id": "AC-003",
    "acceptance_checks": [
      "AC-003"
    ],
    "context": "foundation",
    "category": "foundation",
    "title": "pnpm turbo check-types runs tsc --noEmit across every workspace project",
    "description": "`pnpm turbo check-types` runs `tsc --noEmit` (dashboard uses `tsc --noEmit --project tsconfig.build.json` to skip the Clerk type-test files) across all 7 workspace members in dependency order, exits 0, and prints a per-task success summary. The root `tsconfig.base.json` enables `strict: true` plus `noUncheckedIndexedAccess` and `moduleResolution: bundler`. Re-running it after intentionally introducing a type error inside `packages/shared/src/lib/example.ts` makes the command exit non-zero with a TS2xxx error.",
    "steps": [
      "Step 1: Run `pnpm turbo check-types` and confirm exit 0 with every task reporting \"successful\".",
      "Step 2: Confirm `tsconfig.base.json` enables strict, noUncheckedIndexedAccess, moduleResolution=bundler.",
      "Step 3: Confirm `apps/dashboard/tsconfig.build.json` is referenced so Clerk type tests are excluded."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-004",
    "acceptance_check_id": "AC-004",
    "acceptance_checks": [
      "AC-004"
    ],
    "context": "foundation",
    "category": "foundation",
    "title": "pnpm exec biome check . runs the unified lint+format check; pnpm exec biome check --write . auto-fixes",
    "description": "Biome 2.4.4 (`biome.json` at the repo root) is the single lint+format tool. `pnpm exec biome check .` exits 0 against a clean tree. Introducing a malformed import (e.g. an ungrouped import that should be Biome-sorted) makes `pnpm exec biome check .` exit non-zero with a specific BIOME-### violation. `pnpm exec biome check --write .` re-runs the same check, auto-fixes the violation, and exits 0 on the same tree. ESLint and Prettier configs do not exist anywhere in the repo.",
    "steps": [
      "Step 1: Run `pnpm exec biome check .` and confirm exit 0.",
      "Step 2: Confirm `biome.json` exists at the repo root and Biome 2.4.4 is in devDependencies.",
      "Step 3: Confirm no .eslintrc, .prettierrc, or eslint.config.* file exists anywhere under the repo."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 1,
    "attempts": 1,
    "status": "qa_passed",
    "implemented_at": "2026-07-09T10:54:59Z",
    "implementation_notes": "Verify-first zero-diff checkpoint: pnpm exec biome check . exits 0 (76 warnings, 0 errors). biome.json at repo root with schema 2.4.4, @biomejs/biome ^2.4.4 installed as 2.4.4. No ESLint/Prettier configs found anywhere in repo. All AC-004 steps verified independently at real boundary.",
    "qa_at": "2026-07-09T10:54:59Z",
    "qa_notes": "Independent re-verification: Step 1 - pnpm exec biome check . exit=0, 76 warnings (all warn-level: noNonNullAssertion, noExplicitAny, noArrayIndexKey, useExhaustiveDependencies), 0 errors. Step 2 - biome.json =https://biomejs.dev/schemas/2.4.4/schema.json; @biomejs/biome@^2.4.4 in devDependencies, installed v2.4.4. Step 3 - find . -path ./node_modules -prune -o \\( -name .eslintrc* -o -name .prettierrc* -o -name eslint.config.* -o -name prettier.config.* \\) returns zero results. The orchestrator-claimed feature_list.json formatter error does not reproduce on this tree. All AC-004 steps pass.",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-005",
    "acceptance_check_id": "AC-005",
    "acceptance_checks": [
      "AC-005"
    ],
    "context": "foundation",
    "category": "foundation",
    "title": "pnpm vitest run executes the 7-project test suite",
    "description": "The root `vitest.config.ts` defines 7 projects (shared, forms, analytics, auth, ui, website, dashboard) using a forks pool with a maximum of 3 workers. `pnpm vitest run` (also `pnpm turbo test`) executes the full suite, exits 0 when green, and prints per-project pass/fail counts. The dashboard project has a 15s per-test timeout; the others use the default.",
    "steps": [
      "Step 1: Run `pnpm vitest run` and confirm exit 0 with all 7 projects reporting a pass count.",
      "Step 2: Confirm `vitest.config.ts` declares projects for shared, forms, analytics, auth, ui, website, dashboard.",
      "Step 3: Confirm the dashboard project config sets a 15s per-test timeout."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-006",
    "acceptance_check_id": "AC-006",
    "acceptance_checks": [
      "AC-006"
    ],
    "context": "foundation",
    "category": "foundation",
    "title": "pnpm exec playwright test tests/ runs the E2E suite against chromium",
    "description": "`pnpm exec playwright test tests/` runs the Playwright 1.58.2 E2E suite under `tests/` (audit.spec.ts and visual-functional.spec.ts) against chromium only, 4 viewports (mobile 375×812, tablet 768×1024, desktop 1280×800, wide 1440×900), workers 3, fully parallel, with trace/video/screenshot disabled. The `playwright.config.ts` webServer block auto-starts the production server on port 3000 if not already running. The suite blocks analytics + tracker domains (google-analytics.com, *.clarity.ms, Intercom) in test.beforeEach.",
    "steps": [
      "Step 1: Run `pnpm exec playwright test tests/` and confirm exit 0 with a green report.",
      "Step 2: Confirm `playwright.config.ts` configures chromium-only, 3 workers, fullyParallel, 4 projects for the 4 viewports.",
      "Step 3: Confirm test.beforeEach in `tests/audit.spec.ts` and `tests/visual-functional.spec.ts` routes requests to analytics / clarity / Intercom domains through `page.route()` blocks."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 1,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "browser",
    "observation_methods": [
      "browser"
    ]
  },
  {
    "id": "WI-AC-007",
    "acceptance_check_id": "AC-007",
    "acceptance_checks": [
      "AC-007"
    ],
    "context": "foundation",
    "category": "foundation",
    "title": "pnpm turbo build builds all 7 packages and apps in dependency order",
    "description": "`pnpm turbo build` (root `turbo.json` with `build: { cache: true }`) runs the build for every workspace member in topological dependency order with content-hash caching, exits 0, and emits per-task success output. The website build produces a static export served from CloudFront + S3; the dashboard build runs `tsc --noEmit --project tsconfig.build.json` and emits a Next.js Lambda@Edge bundle. After the build, `.next/` exists at the root of both apps.",
    "steps": [
      "Step 1: Run `pnpm turbo build` and confirm exit 0 with every task successful.",
      "Step 2: Confirm `.next/` exists under `apps/website/` and `apps/dashboard/` after the build.",
      "Step 3: Confirm `turbo.json` sets `build: { cache: true }` and the dashboard's build script invokes `tsc --noEmit --project tsconfig.build.json`."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-009",
    "acceptance_check_id": "AC-009",
    "acceptance_checks": [
      "AC-009"
    ],
    "context": "foundation",
    "category": "functional",
    "title": "Mock-mode fallback: blank CORE_API_URL falls through to the mock Core API client",
    "description": "`apps/dashboard/src/lib/api/get-api-client.ts` resolves to `mock-api-client.ts` when `CORE_API_URL` is unset, and to `http-api-client.ts` otherwise. With `CORE_API_URL` blank in `apps/dashboard/.env.local`, `pnpm --filter dashboard dev` starts and every page that calls `getApiClient()` (analyses, billing, settings, team, integrations, audit, intelligence, topology, memory, notifications, approvals) renders against the mock client without contacting any external service. Setting `CORE_API_URL` to a non-existent host instead causes the same pages to fail-fast with a fetch error.",
    "steps": [
      "Step 1: With `CORE_API_URL` blank, start `pnpm --filter dashboard dev` (port 3001) and load `/dashboard`.",
      "Step 2: Confirm every navigation renders without a network error in the dev log.",
      "Step 3: Set `CORE_API_URL=http://127.0.0.1:1` and confirm the same navigation surfaces a fetch failure in the dev log."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-017",
    "acceptance_check_id": "AC-017",
    "acceptance_checks": [
      "AC-017"
    ],
    "context": "website",
    "category": "functional",
    "title": "Per-context i18n files are deep-merged by apps/website/src/lib/i18n/compose.ts",
    "description": "Each website context owns its translations at `apps/website/src/contexts/{marketing,legal,shell}/infrastructure/i18n/{en,pt-br}.json`. `apps/website/src/lib/i18n/compose.ts` deep-merges all per-context JSON files into a single next-intl message tree at build time. Editing `marketing/infrastructure/i18n/en.json` to add a new key (e.g. `home.hero.cta`) and referencing it via `useTranslations('home.hero')` on the homepage renders the new key; the change is reflected on both `http://localhost:3000/` and `http://localhost:3000/pt-br/` (the latter reads `pt-br.json`).",
    "steps": [
      "Step 1: Confirm the per-context i18n files exist for marketing, legal, and shell in both en.json and pt-br.json.",
      "Step 2: Confirm `apps/website/src/lib/i18n/compose.ts` deep-merges the per-context files.",
      "Step 3: Add a sentinel key to one of the per-context files, rebuild / restart dev, and confirm it surfaces on the matching page in both EN and PT-BR."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-032",
    "acceptance_check_id": "AC-032",
    "acceptance_checks": [
      "AC-032"
    ],
    "context": "dashboard",
    "category": "security",
    "title": "Sentry: 3 beforeSend hooks (server/edge/client) scrub PII; SDK no-ops when DSN is blank",
    "description": "`apps/dashboard/instrumentation.ts` registers the server (`sentry.server.config.ts`) and edge (`sentry.edge.config.ts`) configs; `apps/dashboard/instrumentation-client.ts` registers the client config. All three `beforeSend` hooks apply identical PII scrubbing (auth headers, cookies, request bodies, Clerk tokens) for SOC2 / GDPR / LGPD compliance. `onRequestError` forwards every Next.js error to Sentry. `src/app/global-error.tsx` is the root client error boundary that also calls `Sentry.captureException` in `useEffect`. When `NEXT_PUBLIC_SENTRY_DSN` is blank, the SDK no-ops at init and no network call is made.",
    "steps": [
      "Step 1: Confirm `instrumentation.ts`, `sentry.server.config.ts`, `sentry.edge.config.ts`, `instrumentation-client.ts`, and `global-error.tsx` exist and the three Sentry.*.init() calls reference the same PII scrubbing helper.",
      "Step 2: With `NEXT_PUBLIC_SENTRY_DSN=` empty, load the dashboard and confirm no outbound request to `*.sentry.io`.",
      "Step 3: Set a non-empty DSN, throw a synthetic error, and confirm the error lands in the Sentry project `causeflow-dashboard` under org `causeflow-ai`."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-039",
    "acceptance_check_id": "AC-039",
    "acceptance_checks": [
      "AC-039"
    ],
    "context": "testing_and_observability",
    "category": "testing",
    "title": "Vitest 7 projects with forks pool, max 3 workers, 15s per-test timeout on the dashboard project",
    "description": "The root `vitest.config.ts` defines 7 projects (shared, forms, analytics, auth, ui, website, dashboard) with a `pool: 'forks'` worker model and `poolOptions: { forks: { maxForks: 3 } }`. The dashboard project sets `testTimeout: 15000`; the other projects use the default. `pnpm vitest run` runs all 7 projects and emits a per-project pass/fail summary. `apps/dashboard/package.json` lists `vitest` as a dev dependency.",
    "steps": [
      "Step 1: Confirm `vitest.config.ts` defines projects for shared, forms, analytics, auth, ui, website, dashboard.",
      "Step 2: Confirm `pool: 'forks'` and `poolOptions: { forks: { maxForks: 3 } }` are set.",
      "Step 3: Confirm the dashboard project has `testTimeout: 15000` and the others use the default."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-042",
    "acceptance_check_id": "AC-042",
    "acceptance_checks": [
      "AC-042"
    ],
    "context": "testing_and_observability",
    "category": "observability",
    "title": "pino structured JSON logs in apps/dashboard/src/lib/logger.ts; never logs raw tokens or PII",
    "description": "`apps/dashboard/src/lib/logger.ts` exports a pino 10.3.1 logger configured for structured JSON output. The logger redacts auth headers, Clerk session tokens, and Stripe secrets. `withAuth` and every API handler that catches a non-recoverable error also call `dashLogger.error` with a structured payload (method, path, userId, tenantId, duration) and forward the error to Sentry. The Sentry `beforeSend` hooks apply the same redaction in the observability layer.",
    "steps": [
      "Step 1: Confirm `apps/dashboard/src/lib/logger.ts` exports a pino logger and `apps/dashboard/package.json` lists `pino@^10.3.1` as a dependency.",
      "Step 2: Trigger a logged error in a dashboard API route and confirm the dev log shows structured JSON with method / path / userId / tenantId, and no JWT, Authorization header, or Stripe secret in the payload.",
      "Step 3: Confirm the Sentry beforeSend hooks in instrumentation-client.ts, sentry.server.config.ts, and sentry.edge.config.ts apply the same redaction."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-010",
    "acceptance_check_id": "AC-010",
    "acceptance_checks": [
      "AC-010"
    ],
    "context": "website",
    "category": "functional",
    "title": "9 EN routes return 200 and mirror under /pt-br/",
    "description": "The website exposes 9 EN routes (/, /product, /security, /integrations, /pricing, /use-cases, /from-opsgenie, /privacy, /terms) each returning HTTP 200. The same 9 routes are mirrored under /pt-br/ (so /pt-br/, /pt-br/product, ... /pt-br/terms) and return 200 with the Portuguese content. Each page renders the localized content from the per-context i18n files at `apps/website/src/contexts/{marketing,legal,shell}/infrastructure/i18n/pt-br.json`.",
    "steps": [
      "Step 1: With the dev server up, `for path in / /product /security /integrations /pricing /use-cases /from-opsgenie /privacy /terms; do curl -s -o /dev/null -w \"$path %{http_code}\\n\" http://localhost:3000$path; done` and confirm every line ends in 200.",
      "Step 2: Repeat with `http://localhost:3000/pt-br$path` and confirm every line ends in 200.",
      "Step 3: Confirm the response body for `/pricing` vs `/pt-br/pricing` contains different language (e.g. English vs Portuguese copy)."
    ],
    "depends_on": [
      "AC-002"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-012",
    "acceptance_check_id": "AC-012",
    "acceptance_checks": [
      "AC-012"
    ],
    "context": "website",
    "category": "functional",
    "title": "Website middleware pipeline: crawler detection → staging auth → geo-redirect → Accept-Language → NEXT_LOCALE cookie",
    "description": "`apps/website/src/middleware.ts` runs the documented pipeline. (1) Crawler detection: 45+ bot user-agent substrings (googlebot, bingbot, slurp, ...) never get redirected. A request with `User-Agent: Googlebot/2.1` to `/` returns 200 with the canonical EN URL, not a geo-redirect. (2) Staging auth: when `NEXT_PUBLIC_DEPLOYMENT_STAGE === 'staging'`, requests without the staging cookie get bounced to `/staging-auth`. (3) Geo-redirect: a request with `Cloudfront-Viewer-Country: BR` to `/` returns a 307 to `/pt-br/`. (4) Accept-Language: a request with `Accept-Language: pt-BR,en;q=0.8` to `/` returns 307 to `/pt-br/`. (5) NEXT_LOCALE cookie: a request with the cookie set to `pt-br` to `/` returns 307 to `/pt-br/`, and the cookie carries `Max-Age=31536000`.",
    "steps": [
      "Step 1: Send `curl -H 'User-Agent: Googlebot/2.1' http://localhost:3000/` and confirm the response is 200, not a redirect.",
      "Step 2: Send `curl -H 'Cloudfront-Viewer-Country: BR' http://localhost:3000/` and confirm a 307 to /pt-br/.",
      "Step 3: Send `curl -H 'Accept-Language: pt-BR,en;q=0.8' -i http://localhost:3000/` and confirm a 307 to /pt-br/; then re-send with `Cookie: NEXT_LOCALE=pt-br` and confirm 307 with `Set-Cookie: NEXT_LOCALE=pt-br; Max-Age=31536000`."
    ],
    "depends_on": [
      "AC-002"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-013",
    "acceptance_check_id": "AC-013",
    "acceptance_checks": [
      "AC-013"
    ],
    "context": "website",
    "category": "security",
    "title": "Website security headers: X-Frame-Options DENY, CSP, HSTS 2y+preload, Referrer-Policy, Permissions-Policy",
    "description": "Every response from the website (including SSG pages) carries: `X-Frame-Options: DENY`; a `Content-Security-Policy` that allow-lists `self`, GA4 (`https://www.googletagmanager.com`, `https://www.google-analytics.com`), Microsoft Clarity (`https://*.clarity.ms`), and Loops (`https://app.loops.so`); `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload`; `Referrer-Policy: strict-origin-when-cross-origin`; and a `Permissions-Policy` that disables camera, microphone, and geolocation. Headers are defined in `apps/website/next.config.mjs` and apply to every route.",
    "steps": [
      "Step 1: `curl -I http://localhost:3000/` and confirm every required header is present with the documented value.",
      "Step 2: Confirm `apps/website/next.config.mjs` defines the headers() block with the documented CSP, HSTS, Referrer-Policy, and Permissions-Policy values.",
      "Step 3: Send the same request to `/pt-br/` and confirm the header set is identical."
    ],
    "depends_on": [
      "AC-002"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-014",
    "acceptance_check_id": "AC-014",
    "acceptance_checks": [
      "AC-014"
    ],
    "context": "website",
    "category": "functional",
    "title": "/get-started is retired; redirects to the dashboard's real sign-up URL per stage",
    "description": "`/get-started` is no longer a valid route in the website. `next.config.mjs` declares a `redirects()` block that maps `/get-started` (and `/pt-br/get-started`) to the per-stage sign-up URL: production → `https://dashboard.causeflow.ai/auth/sign-up`; staging → `https://dashboard-staging.causeflow.ai/auth/sign-up`. `curl -I http://localhost:3000/get-started` returns 307/308 with a Location header pointing at the dashboard sign-up URL.",
    "steps": [
      "Step 1: `curl -I http://localhost:3000/get-started` and confirm a 307/308 with Location set to the dashboard sign-up URL.",
      "Step 2: Confirm the same redirect applies to `/pt-br/get-started`.",
      "Step 3: Confirm `apps/website/next.config.mjs` contains a redirects() entry keyed on `/get-started`."
    ],
    "depends_on": [
      "AC-002"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-015",
    "acceptance_check_id": "AC-015",
    "acceptance_checks": [
      "AC-015"
    ],
    "context": "website",
    "category": "functional",
    "title": "/staging-auth is the password gate for the staging deploy (STAGING_PASSWORD=causeflow-staging-2026)",
    "description": "When the website is deployed to staging, requests to any non-asset URL without the `staging-authorized` cookie are redirected to `/staging-auth`, which renders a password form. Submitting the documented password `causeflow-staging-2026` (set in `apps/website/sst.config.ts`) sets the staging-authorized cookie and redirects back to the originally-requested URL. In local dev (`NEXT_PUBLIC_DEPLOYMENT_STAGE !== 'staging'`), the gate is bypassed.",
    "steps": [
      "Step 1: With `NEXT_PUBLIC_DEPLOYMENT_STAGE=staging` set, `curl -I http://localhost:3000/` and confirm a 307 to `/staging-auth`.",
      "Step 2: Submit the documented password and confirm the staging-authorized cookie is set and the original URL is followed.",
      "Step 3: Confirm the password is read from `apps/website/sst.config.ts` (production + staging) and that local dev bypasses the gate."
    ],
    "depends_on": [
      "AC-002"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-016",
    "acceptance_check_id": "AC-016",
    "acceptance_checks": [
      "AC-016"
    ],
    "context": "website",
    "category": "functional",
    "title": "robots.ts (noindex on staging) + sitemap.ts (XML for all routes×locales) + structured-data.tsx (JSON-LD)",
    "description": "`apps/website/src/app/robots.ts` returns a robots response that disallows crawling (`Disallow: /`) when `NEXT_PUBLIC_DEPLOYMENT_STAGE === 'staging'` and allows it otherwise. `apps/website/src/app/sitemap.ts` generates an XML sitemap that includes every route × locale (EN + PT-BR). `apps/website/src/contexts/marketing/presentation/components/sections/structured-data.tsx` injects JSON-LD blocks (Organization, Website, FAQPage) into the home and product pages — `curl http://localhost:3000/` includes `<script type=\"application/ld+json\">` payloads for each schema.",
    "steps": [
      "Step 1: `curl http://localhost:3000/robots.txt` and confirm a `Disallow: /` line on staging or an empty/allow on production.",
      "Step 2: `curl http://localhost:3000/sitemap.xml` and confirm an XML payload with one entry per route × locale.",
      "Step 3: `curl http://localhost:3000/` and grep for `<script type=\"application/ld+json\">`; confirm the payload parses to JSON with `@type: Organization`."
    ],
    "depends_on": [
      "AC-002"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-018",
    "acceptance_check_id": "AC-018",
    "acceptance_checks": [
      "AC-018"
    ],
    "context": "website",
    "category": "analytics",
    "title": "GA4 + Microsoft Clarity loaded via AnalyticsProvider; no-op when env vars are empty",
    "description": "`packages/analytics/src/infrastructure/ga4/index.ts` exposes `initGA4` and `trackGA4PageView`; `packages/analytics/src/infrastructure/clarity/index.ts` exposes `initClarity`. The AnalyticsProvider mounted by `apps/website/src/app/[locale]/layout.tsx` calls both when `NEXT_PUBLIC_GA4_MEASUREMENT_ID` and `NEXT_PUBLIC_CLARITY_ID` are set, and is a no-op when they are blank. With both env vars empty, the homepage does not make any request to `https://www.google-analytics.com` or `https://www.clarity.ms` (verifiable via Playwright network logging). When set, the page emits a gtag config call and a Clarity init call on first paint.",
    "steps": [
      "Step 1: With both env vars blank, load `/` in Playwright with network logging and confirm zero requests to google-analytics.com or *.clarity.ms.",
      "Step 2: Set `NEXT_PUBLIC_GA4_MEASUREMENT_ID=G-TEST` and `NEXT_PUBLIC_CLARITY_ID=TEST`, reload `/`, and confirm a gtag config + Clarity init request are made.",
      "Step 3: Confirm the AnalyticsProvider is mounted at the website root layout and the analytics package never imports from contexts that hold auth tokens."
    ],
    "depends_on": [
      "AC-002"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "browser",
    "observation_methods": [
      "browser"
    ]
  },
  {
    "id": "WI-AC-019",
    "acceptance_check_id": "AC-019",
    "acceptance_checks": [
      "AC-019"
    ],
    "context": "dashboard",
    "category": "security",
    "title": "Clerk middleware: unauthenticated /dashboard access redirects to /auth/sign-in",
    "description": "`apps/dashboard/src/middleware.ts` is wrapped in `clerkMiddleware()` from `@clerk/nextjs/server`. A request to `http://localhost:3001/dashboard` (or any non-public route) without a Clerk session returns a 307 to `/auth/sign-in`. `GET /api/health/detailed` and `/api/billing/webhook` are explicitly public-route-matched and skip auth. After a successful Clerk sign-in the original URL is followed.",
    "steps": [
      "Step 1: With the dev server up, `curl -I http://localhost:3001/dashboard` and confirm a 307 Location to `/auth/sign-in`.",
      "Step 2: Confirm the same redirect happens for `/dashboard/analyses`, `/dashboard/billing`, and `/dashboard/integrations`.",
      "Step 3: Confirm `GET /api/health/detailed` returns 200 without a session."
    ],
    "depends_on": [
      "AC-002"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 1,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-027",
    "acceptance_check_id": "AC-027",
    "acceptance_checks": [
      "AC-027"
    ],
    "context": "dashboard",
    "category": "functional",
    "title": "15 integration types are surfaced as connection cards in the dashboard's integrations page",
    "description": "`apps/dashboard/src/contexts/integrations/domain/types.ts` enumerates 15 integration types: Slack, GitHub, Jira, AWS CloudWatch, HubSpot, Trello, PostgreSQL, Linear, Sentry, MongoDB, Datadog, PagerDuty, Grafana, Confluence, Webhooks. The integration catalog page renders one connection card per type with the Composio logo, the integration description, and a \"Connect\" CTA. Each integration has a type-specific Zod schema in `apps/dashboard/src/lib/api/schemas.ts`; credentials are forwarded to the Core API for KMS-envelope encryption and DynamoDB storage.",
    "steps": [
      "Step 1: Sign in as admin, load `/dashboard/integrations`, and confirm 15 integration cards are rendered (Slack, GitHub, Jira, AWS CloudWatch, HubSpot, Trello, PostgreSQL, Linear, Sentry, MongoDB, Datadog, PagerDuty, Grafana, Confluence, Webhooks).",
      "Step 2: Confirm `Integration` type carries a `composioTriggerId` field and the integration-domain types file lists exactly 15 type identifiers.",
      "Step 3: Confirm the dashboard's `next.config.mjs` `images.remotePatterns` allow-lists `logos.composio.dev` and `backend.composio.dev`."
    ],
    "depends_on": [
      "AC-002"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": false,
    "integration": false,
    "retries": 1,
    "attempts": 1,
    "status": "implemented",
    "implemented_at": "2026-07-09T16:55:00Z",
    "implementation_notes": "Verify-first checkpoint: types.ts has all 15 AC-specified types (plus 2 extras: Notion, Shortcut). INTEGRATION_CATALOG has 17 entries with descriptions, local SVG icons, and phase-gated Connect CTAs. Type-specific Zod schemas exist in per-context api-schema.ts (re-exported through lib/api/schemas.ts). Integration type has encryptedCredentials field for KMS-envelope encryption. Dev server boots (port 5181), /api/integrations/catalog route exists and responds 401 without auth, tsc --noEmit exit 0, 163 test files/1071 tests pass. Composio-specific requirements (logos, composioTriggerId, remotePatterns) intentionally removed per AC-051 open-source-local-runtime migration -- cards use local SVGs instead of Composio CDN.",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-034",
    "acceptance_check_id": "AC-034",
    "acceptance_checks": [
      "AC-034"
    ],
    "context": "foundation",
    "category": "ops",
    "title": "GitHub Actions: 2 workflows (website-deploy.yml, dashboard-deploy.yml) with frozen-lockfile + type-check + lint + test + build + SST deploy",
    "description": "`.github/workflows/website-deploy.yml` and `.github/workflows/dashboard-deploy.yml` are the two deploy workflows. Each sets up pnpm via `pnpm/action-setup@v4`, Node via `.nvmrc` (24), installs with `--frozen-lockfile`, then runs `pnpm turbo check-types`, `pnpm turbo lint`, `pnpm turbo test`, `pnpm turbo build` before triggering the SST deploy. Staging deploys trigger on push to main; production deploys trigger via `workflow_dispatch` with `inputs.stage=production`. The workflows use `environment: staging` / `environment: production` and `concurrency:` to avoid racing deploys.",
    "steps": [
      "Step 1: Confirm both workflow files exist under `.github/workflows/`, each referencing pnpm/action-setup@v4 and --frozen-lockfile.",
      "Step 2: Confirm each workflow runs check-types, lint, test, build in sequence before the SST deploy step.",
      "Step 3: Confirm each workflow sets `environment: staging` and `environment: production` plus a `concurrency` block."
    ],
    "depends_on": [
      "AC-007"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-011",
    "acceptance_check_id": "AC-011",
    "acceptance_checks": [
      "AC-011"
    ],
    "context": "website",
    "category": "functional",
    "title": "next-intl wiring: ['en','pt-br'] locales, 'as-needed' prefix, hreflang tags, language selector",
    "description": "`apps/website/src/i18n/routing.ts` declares `locales: ['en','pt-br']`, `defaultLocale: 'en'`, `localePrefix: 'as-needed'`, `localeDetection: false`. `lib/metadata.ts#generatePageMetadata()` emits canonical + hreflang alternates on every page; the response body for `/` contains a `<link rel=\"alternate\" hreflang=\"en\" ...>`, a `<link rel=\"alternate\" hreflang=\"pt-br\" ...>`, and a `<link rel=\"alternate\" hreflang=\"x-default\" ...>`. The header renders a language selector that links to the same page under the other locale.",
    "steps": [
      "Step 1: `curl http://localhost:3000/` and grep the response for `hreflang=\"en\"`, `hreflang=\"pt-br\"`, `hreflang=\"x-default\"`.",
      "Step 2: Confirm `apps/website/src/i18n/routing.ts` matches the documented locale config.",
      "Step 3: Load the homepage in a browser and confirm the header renders a language selector linking to `/pt-br/`."
    ],
    "depends_on": [
      "AC-010"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-020",
    "acceptance_check_id": "AC-020",
    "acceptance_checks": [
      "AC-020"
    ],
    "context": "dashboard",
    "category": "functional",
    "title": "Dashboard middleware pipeline: staging auth → public bypass → clerkMiddleware → profile guard → i18n",
    "description": "`apps/dashboard/src/middleware.ts` runs the documented pipeline in order. The `isPublicRoute` matcher includes `/api/health(.*)`, `/api/ingestion/webhook`, `/auth/(.*)`, `/accept-invitation(.*)`, `/beta-waitlist(.*)`, `/waitlist(.*)`, `/create-organization(.*)`, and the locale-prefixed equivalents. A request to a non-public route with a valid Clerk session but no Clerk organization is redirected to `/create-organization`. Authenticated requests that have an org reach the page; unauthenticated requests are redirected to `/auth/sign-in` before the org check runs.",
    "steps": [
      "Step 1: Confirm `isPublicRoute` in `apps/dashboard/src/middleware.ts` matches every documented public route + its locale-prefixed variant.",
      "Step 2: Sign in with a Clerk session that has no `orgId` and confirm navigation to `/dashboard` is redirected to `/create-organization`.",
      "Step 3: Sign in with a Clerk session that has an `orgId` and confirm the dashboard renders without a redirect."
    ],
    "depends_on": [
      "AC-019"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-021",
    "acceptance_check_id": "AC-021",
    "acceptance_checks": [
      "AC-021"
    ],
    "context": "dashboard",
    "category": "security",
    "title": "RBAC: 9 permissions × 2 roles enforced at API level (withAuth) and component level (RoleGuard)",
    "description": "`apps/dashboard/src/contexts/identity/domain/rbac/permissions.ts` defines 9 permissions (MANAGE_TEAM, MANAGE_INTEGRATIONS, MANAGE_BILLING, MANAGE_SETTINGS, SUBMIT_ANALYSIS, VIEW_ANALYSES, VIEW_INTEGRATIONS, VIEW_BILLING, VIEW_SETTINGS, VIEW_TEAM) and the `hasPermission(role, permission)` predicate. The role map assigns all 9 to `admin` and 5 read/submit permissions to `member`. `withAuth(handler, { adminOnly: true })` returns 403 for member; `withAuth(handler, { requiredRole: 'admin' })` does the same. `RoleGuard` + the `usePermission` hook hide admin-only UI controls for members. `requirePermission(role, perm)` throws server-side before the handler runs.",
    "steps": [
      "Step 1: Confirm `permissions.ts` defines exactly 9 permission constants and the admin / member permission arrays match the documented sets.",
      "Step 2: As a member, `POST /api/billing/checkout` (adminOnly) returns 403; the same call as an admin returns the proxied response from Core.",
      "Step 3: Render a page that wraps an admin-only button in `<RoleGuard permission=\"manage_billing\">` and confirm the button is hidden for member and visible for admin."
    ],
    "depends_on": [
      "AC-019"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-022",
    "acceptance_check_id": "AC-022",
    "acceptance_checks": [
      "AC-022"
    ],
    "context": "dashboard",
    "category": "functional",
    "title": "Credits system: free plan = 3 credits/month lazy-renewed every 30 days; 402 CREDITS_EXHAUSTED on exhaustion; plans.ts is the single source of truth",
    "description": "`packages/shared/src/domain/constants/plans.ts` is the single source of truth for plan catalog (starter=15, pro=60, business=200 credits/month, enterprise=unlimited). The free plan grants 3 credits/month, renewed lazily on the next `GET /api/metrics` call once 30 days have elapsed. Submitting a new analysis decrements by 1; when credits reach 0, `POST /api/incidents` returns `402` with `{ code: 'CREDITS_EXHAUSTED' }`. Credits stay active during `canceling` status until `currentPeriodEnd`.",
    "steps": [
      "Step 1: Confirm `packages/shared/src/domain/constants/plans.ts` defines the documented plan catalog and `getCreditsForPlan()` returns the documented counts.",
      "Step 2: As a free-plan tenant with 0 credits, `POST /api/incidents` returns 402 with body `{ \"code\": \"CREDITS_EXHAUSTED\" }`.",
      "Step 3: As a free-plan tenant with 1 credit remaining, `POST /api/incidents` returns 200 and a subsequent call returns 402 CREDITS_EXHAUSTED."
    ],
    "depends_on": [
      "AC-019"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-024",
    "acceptance_check_id": "AC-024",
    "acceptance_checks": [
      "AC-024"
    ],
    "context": "dashboard",
    "category": "functional",
    "title": "44 API routes: each route delegates to contexts/&lt;name&gt;/api/&lt;name&gt;-handler.ts",
    "description": "The dashboard's `src/app/api/` directory contains 44 route handlers. Every file is a thin re-export that delegates to a context's `api/<name>-handler.ts`. Routes are grouped by context: billing (checkout, portal, webhook, subscription, usage), settings (settings, api-keys), incidents (list, feedback), approvals (list, pending, respond), relay (status), memory (insights, summary, ask), topology (list, [id], health), health (detailed), notifications (list, patch), pattern-analytics, onboarding (complete-profile, connect-integration), team (list, invite, invites, invite-email, user, user-role), integrations (list, type, oauth-authorize, oauth-callback, oauth-status, oauth-token, sentry, slack-oauth, slack-config, github-*, triggers-available, triggers, trigger-id, test-connection, aws-setup).",
    "steps": [
      "Step 1: `find apps/dashboard/src/app/api -name 'route.ts' | wc -l` returns 44 (or, after the documented changes, the documented count).",
      "Step 2: Confirm every `route.ts` is a 1-line re-export of a context handler.",
      "Step 3: Confirm `apps/dashboard/src/contexts/*/api/*-handler.ts` exists for every route."
    ],
    "depends_on": [
      "AC-019"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-029",
    "acceptance_check_id": "AC-029",
    "acceptance_checks": [
      "AC-029"
    ],
    "context": "dashboard",
    "category": "security",
    "title": "withAuth: 401/403/429 enforcement at the API boundary",
    "description": "`apps/dashboard/src/lib/api/with-auth.ts#withAuth(handler, options)` enforces, in order: (1) Clerk auth — no session returns 401 with `{ error: 'Authentication required.' }`; (2) tenant validation — no `orgId` returns 403 with `{ error: 'Profile setup required. Please complete onboarding.' }` (skippable with `allowNoOrg: true`); (3) role guard — `adminOnly: true` or `requiredRole: 'admin'` returns 403 with `{ error: 'Admin access required.' }` / `{ error: 'Insufficient role for this action.' }`; (4) per-user rate limit — 60 req/min default, 429 with `Retry-After` header on excess; (5) staff-only — `staffOnly: true` returns 403 for non-staff emails. The wrapped handler runs only after all guards pass.",
    "steps": [
      "Step 1: With no session, `GET /api/settings` returns 401 with the documented body.",
      "Step 2: With a session but no `orgId`, `GET /api/settings` returns 403 with the documented body.",
      "Step 3: With a member session, `POST /api/billing/checkout` (adminOnly) returns 403; with 70 requests in 60s the 70th returns 429 with a `Retry-After` header."
    ],
    "depends_on": [
      "AC-019"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-030",
    "acceptance_check_id": "AC-030",
    "acceptance_checks": [
      "AC-030"
    ],
    "context": "dashboard",
    "category": "security",
    "title": "BFF pattern: Clerk session JWT forwarded as Bearer to Core API; never exposed to the browser",
    "description": "`apps/dashboard/src/lib/api/get-backend-token.ts` reads the Clerk session JWT server-side via `getToken()` from `@clerk/nextjs/server`. The dashboard's `http-api-client.ts` attaches it as `Authorization: Bearer <jwt>` on every Core API request. The browser never sees the token: the React tree only renders Core API responses, never the JWT itself. The HTTP client also strips auth headers from any error that reaches `Sentry.captureException`. The dashboard's `.env.local` lists no `STRIPE_SECRET_KEY` or any other Core-side secret — only `NEXT_PUBLIC_*` and `CLERK_SECRET_KEY`.",
    "steps": [
      "Step 1: Sign in, then in the browser dev tools confirm no `__session` JWT, no `Authorization` header on outbound requests, and no JWT in any rendered HTML.",
      "Step 2: Tail the dev log while loading a dashboard page and confirm every Core API call carries `Authorization: Bearer ...` server-side.",
      "Step 3: Confirm `apps/dashboard/src/lib/api/http-api-client.ts` scrubs `Authorization` from any error it forwards to Sentry."
    ],
    "depends_on": [
      "AC-019"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "browser",
    "observation_methods": [
      "browser"
    ]
  },
  {
    "id": "WI-AC-023",
    "acceptance_check_id": "AC-023",
    "acceptance_checks": [
      "AC-023"
    ],
    "context": "dashboard",
    "category": "functional",
    "title": "Plan gate: active only when status ∈ {active, trialing} AND currentPeriodEnd is set",
    "description": "`apps/dashboard/src/contexts/billing/application/plan-status.ts#fetchPlanStatus` returns `hasActivePlan: true` only when the Core API's `GET /v1/billing/subscription` returns `status ∈ {active, trialing}` AND `currentPeriodEnd` is set. Fresh tenants whose Core subscription has `status='active'` but no `currentPeriodEnd` (because they have never completed Stripe checkout) return `hasActivePlan: false`, which the dashboard layout uses to render the plan gate. The Core API default of `status='active'` for never-subscribed tenants is overridden by the missing `currentPeriodEnd` check.",
    "steps": [
      "Step 1: As a fresh tenant with Core returning `{ status: 'active', currentPeriodEnd: null }`, `getPlanStatus()` returns `hasActivePlan: false` and the dashboard layout renders the plan gate.",
      "Step 2: As a paid tenant with Core returning `{ status: 'active', currentPeriodEnd: '2026-12-31T...' }`, `getPlanStatus()` returns `hasActivePlan: true` and the dashboard renders.",
      "Step 3: Confirm `plan-status.ts` documents the cross-check rationale (the Core API defaults to active for fresh tenants)."
    ],
    "depends_on": [
      "AC-022"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-025",
    "acceptance_check_id": "AC-025",
    "acceptance_checks": [
      "AC-025"
    ],
    "context": "dashboard",
    "category": "functional",
    "title": "Live SSE feed: incident detail pages forward the Core API's streaming response",
    "description": "The incident detail page (`/dashboard/analyses/[id]`) opens a Server-Sent Events stream that forwards the Core API's `/v1/incidents/:id/stream` as a streaming Next.js response. The Clerk middleware does not block streaming responses; the dashboard's API route handler returns a `Response` with `Content-Type: text/event-stream` and pipes Core's events through. A running investigation emits per-agent events (log, metric, change, code, infra, db) to the client as they complete.",
    "steps": [
      "Step 1: Open an incident detail page with a running investigation and confirm the browser opens an EventSource (or fetch streaming) connection to the dashboard's incident API.",
      "Step 2: Confirm the API route uses `Content-Type: text/event-stream` and proxies events from the Core API.",
      "Step 3: Confirm the Clerk middleware's `isPublicRoute` matcher does not block the streaming endpoint and the API route returns a streaming Response, not a buffered JSON one."
    ],
    "depends_on": [
      "AC-024"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-026",
    "acceptance_check_id": "AC-026",
    "acceptance_checks": [
      "AC-026"
    ],
    "context": "dashboard",
    "category": "functional",
    "title": "lvh.me loopback rewrite: localhost → lvh.me for Stripe callbacks to bypass the Core WAF SSRF filter",
    "description": "Core's WAF rejects Stripe callbacks containing `localhost` or `127.0.0.1` in the URL. The dashboard's checkout and portal handlers rewrite the URL host from `localhost` to `lvh.me` (a public-DNS loopback alias) before calling Core. The dev middleware (`apps/dashboard/src/middleware.ts`) does the inverse rewrite — a request with hostname `lvh.me` in dev is redirected to `localhost` so the Clerk session cookie is honored. Production uses the real dashboard hostname; the rewrite is dev-only.",
    "steps": [
      "Step 1: Confirm `apps/dashboard/src/middleware.ts` redirects `lvh.me` → `localhost` in dev.",
      "Step 2: Confirm the checkout / portal handlers rewrite `localhost` → `lvh.me` before calling Core.",
      "Step 3: In dev, set `CORE_API_URL=http://api.lvh.me:9999` (or similar) and confirm the handlers' outbound URL uses the lvh.me host."
    ],
    "depends_on": [
      "AC-024"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-031",
    "acceptance_check_id": "AC-031",
    "acceptance_checks": [
      "AC-031"
    ],
    "context": "dashboard",
    "category": "security",
    "title": "Stripe: dashboard React SDK loads PaymentElement with the publishable key; secret key never in dashboard runtime env",
    "description": "The dashboard's runtime Stripe dependency is `@stripe/react-stripe-js` (Elements, PaymentElement, useStripe, useElements) and `@stripe/stripe-js` (loadStripe). The billing modal at `apps/dashboard/src/contexts/billing/presentation/components/payment-modal.tsx` mounts a Stripe PaymentElement when `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` is set. Checkout, portal, and webhook handlers do not call Stripe directly — they call the Core API, which owns `STRIPE_SECRET_KEY` and webhook signature verification. The dashboard's `.env.local` and SST env injection list no `STRIPE_SECRET_KEY`. The Stripe Server SDK (`stripe@20.x`) is installed as a dev dependency for the CLI scripts only.",
    "steps": [
      "Step 1: With `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` set, open the billing modal and confirm the Stripe PaymentElement iframe loads.",
      "Step 2: Confirm `STRIPE_SECRET_KEY` is NOT listed in `apps/dashboard/.env.example` and NOT referenced in `apps/dashboard/sst.config.ts` env injection.",
      "Step 3: Confirm `stripe` appears in `devDependencies` (not `dependencies`) of `apps/dashboard/package.json` and is only imported from `apps/dashboard/scripts/setup-stripe.ts` and `apps/dashboard/scripts/delete-user.ts`."
    ],
    "depends_on": [
      "AC-024"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "cli",
    "observation_methods": [
      "cli"
    ]
  },
  {
    "id": "WI-AC-035",
    "acceptance_check_id": "AC-035",
    "acceptance_checks": [
      "AC-035"
    ],
    "context": "dashboard",
    "category": "functional",
    "title": "Composio: 15 integration types; image allow-list for logos.composio.dev and backend.composio.dev",
    "description": "The dashboard's integration catalog uses Composio for third-party integration metadata. The 15 integration types (Slack, GitHub, Jira, AWS CloudWatch, HubSpot, Trello, PostgreSQL, Linear, Sentry, MongoDB, Datadog, PagerDuty, Grafana, Confluence, Webhooks) are surfaced as connection cards in the dashboard. The `Integration` domain type carries a `composioTriggerId` field. The dashboard's `next.config.mjs` `images.remotePatterns` allow-lists `logos.composio.dev` and `backend.composio.dev`. No Composio API key lives in this repo — the actual Composio connection flow runs on the Core API; the dashboard only references Composio's metadata endpoints via the image allow-list.",
    "steps": [
      "Step 1: Confirm `apps/dashboard/next.config.mjs` lists `logos.composio.dev` and `backend.composio.dev` in `images.remotePatterns`.",
      "Step 2: Confirm `apps/dashboard/src/contexts/integrations/domain/types.ts` defines the 15 integration types and the `Integration` type carries `composioTriggerId`.",
      "Step 3: Confirm no `COMPOSIO_API_KEY` env var is referenced in `apps/dashboard/.env.example` or `apps/dashboard/sst.config.ts`."
    ],
    "depends_on": [
      "AC-024"
    ],
    "verify_first": true,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-040",
    "acceptance_check_id": "AC-040",
    "acceptance_checks": [
      "AC-040"
    ],
    "context": "testing_and_observability",
    "category": "testing",
    "title": "Playwright 4 viewports (mobile 375×812, tablet 768×1024, desktop 1280×800, wide 1440×900) chromium only, workers 3, fully parallel, trace/video/screenshot off",
    "description": "`playwright.config.ts` configures Playwright 1.58.2 with `projects:` for 4 viewports (mobile 375×812, tablet 768×1024, desktop 1280×800, wide 1440×900), chromium only, `workers: 3`, `fullyParallel: true`, and `trace: 'off'`, `video: 'off'`, `screenshot: 'off'`. The `webServer` block auto-starts the production server on port 3000 if not already running. The configuration is the single Playwright config for both apps' E2E coverage.",
    "steps": [
      "Step 1: Confirm `playwright.config.ts` declares 4 projects with the documented viewport sizes and chromium-only.",
      "Step 2: Confirm `workers: 3`, `fullyParallel: true`, `trace: 'off'`, `video: 'off'`, `screenshot: 'off'`.",
      "Step 3: Confirm the `webServer` block starts the production server on port 3000."
    ],
    "depends_on": [
      "AC-001"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "browser",
    "observation_methods": [
      "browser"
    ]
  },
  {
    "id": "WI-AC-041",
    "acceptance_check_id": "AC-041",
    "acceptance_checks": [
      "AC-041"
    ],
    "context": "testing_and_observability",
    "category": "testing",
    "title": "Playwright test.beforeEach blocks analytics + tracker domains (GA4, Clarity, Intercom)",
    "description": "`tests/audit.spec.ts` and `tests/visual-functional.spec.ts` declare a `test.beforeEach` that uses `page.route()` to block any request to `https://www.google-analytics.com`, `https://*.clarity.ms`, `https://clarity.ms`, and Intercom. This prevents test traffic from polluting production analytics. The block list covers both gtag's `collect` endpoint and Clarity's session-replay endpoints.",
    "steps": [
      "Step 1: `grep -n 'page.route' tests/audit.spec.ts tests/visual-functional.spec.ts` returns route blocks for google-analytics.com, clarity.ms, and Intercom.",
      "Step 2: Run `pnpm exec playwright test tests/audit.spec.ts` with a console listener and confirm no request to those domains is made.",
      "Step 3: Confirm the block list matches the documented domains in the spec (GA, Clarity, Intercom)."
    ],
    "depends_on": [
      "AC-040"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "attempts": 0,
    "status": "qa-passed",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-043",
    "acceptance_check_id": "AC-043",
    "acceptance_checks": [
      "AC-043"
    ],
    "context": "testing_and_observability",
    "category": "testing",
    "title": "Verification protocol for dashboard changes: local Playwright with mock client → staging login via env.staging creds → aws logs tail /ecs/causeflow-staging-api --follow",
    "description": "`apps/dashboard/CLAUDE.md` documents the verification protocol: (1) local sanity via `pnpm --filter dashboard dev` (port 3001) with blank `CORE_API_URL` (mock client), Playwright codegen or scripted specs, screenshots into `.artifacts/playwright/screenshots/YYYY-MM-DD_HHmm/`; (2) staging verification via the test creds in `web/apps/dashboard/.env.staging` (`STAGING_TEST_USER`, `STAGING_TEST_PASSWORD`) on `https://dashboard-staging.causeflow.ai`, with `aws logs tail /ecs/causeflow-staging-api --follow` running in parallel; (3) E2E coverage — every PR that touches dashboard UI or APIs ships a Playwright spec; (4) manual user steps (Sentry Internal Integration, GitHub OAuth app, Slack workspace approval) are surfaced in the PR description and the agent pauses for user confirmation. The `.env.staging` file holds login creds only, not runtime env.",
    "steps": [
      "Step 1: Confirm `apps/dashboard/CLAUDE.md` documents the four-step verification protocol with the exact URL, AWS log group, and env-staging path.",
      "Step 2: Confirm `apps/dashboard/.env.staging` exists, contains `STAGING_TEST_USER` and `STAGING_TEST_PASSWORD`, and is excluded from runtime env.",
      "Step 3: Confirm `apps/dashboard/.gitignore` does NOT exclude `.env.staging` (Playwright reads it; the file is intentionally tracked) and the `kill_command` in the repo's CLAUDE.md kills both Next.js and Playwright processes before any test."
    ],
    "depends_on": [
      "AC-040",
      "AC-041"
    ],
    "verify_first": true,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 1,
    "attempts": 0,
    "status": "verify-first",
    "observation_method": "browser",
    "observation_methods": [
      "browser"
    ]
  },
  {
    "id": "WI-AC-044",
    "context": "open-source-local-runtime",
    "category": "foundation",
    "description": "From a clean monorepo root with a blank shell env (no `CLERK_*`, `STRIPE_*`, `AWS_*`, `SENTRY_*`, `LOOPS_*`, `CORE_API_URL`, or SST-injected vars set), `docker compose up -d` brings up `causeflow-postgres`, `redis`, `hindsight`, `causeflow-api`, `causeflow-worker`, `causeflow-website` (port 3000), and `causeflow-dashboard` (port 3001) per the bundled `docker-compose.yml`. Within 90 seconds `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/` returns 200 and `curl -s -o /dev/null -w '%{http_code}' http://localhost:3001/auth/sign-in` returns 200 (the dashboard's local sign-in form, not the Clerk-hosted UI). Grepping the dashboard container's boot log for `clerk.com`, `stripe.com`, `amazonaws.com`, `sentry.io`, or `sst.` returns zero outbound URLs.",
    "steps": [
      "Verify AC-044: From a clean monorepo root with a blank shell env (no `CLERK_*`, `STRIPE_*`, `AWS_*`, `SENTRY_*`, `LOOPS_*`, `CORE_API_URL`, or SST-injected vars set), `docker compose up -d` brings up `causeflow-postgres`, `redis`, `hindsight`, `causeflow-api`, `causeflow-worker`, `causeflow-website` (port 3000), and `causeflow-dashboard` (port 3001) per the bundled `docker-compose.yml`. Within 90 seconds `curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/` returns 200 and `curl -s -o /dev/null -w '%{http_code}' http://localhost:3001/auth/sign-in` returns 200 (the dashboard's local sign-in form, not the Clerk-hosted UI). Grepping the dashboard container's boot log for `clerk.com`, `stripe.com`, `amazonaws.com`, `sentry.io`, or `sst.` returns zero outbound URLs."
    ],
    "acceptance_checks": [
      "AC-044"
    ],
    "depends_on": [],
    "verify_first": false,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-045",
    "context": "open-source-local-runtime",
    "category": "foundation",
    "description": "`apps/website/.env.example` ships with only `NEXT_PUBLIC_GA4_MEASUREMENT_ID` and `NEXT_PUBLIC_CLARITY_ID` (both optional, blank = no-op). `apps/dashboard/.env.example` ships with only `CORE_API_URL` (default `http://core-api:3099` for the docker-compose network), `JWT_SECRET` (shared with the Core container, must match the Core's `JWT_SECRET`), and the optional `NEXT_PUBLIC_*` analytics keys. `CLERK_*`, `STRIPE_*`, `AWS_*`, `SENTRY_*` (build-time), `LOOPS_*`, and any SST-injected var are absent from both files. `LOOPS_API_KEY` is removed from `apps/website/.env.example`.",
    "steps": [
      "Verify AC-045: `apps/website/.env.example` ships with only `NEXT_PUBLIC_GA4_MEASUREMENT_ID` and `NEXT_PUBLIC_CLARITY_ID` (both optional, blank = no-op). `apps/dashboard/.env.example` ships with only `CORE_API_URL` (default `http://core-api:3099` for the docker-compose network), `JWT_SECRET` (shared with the Core container, must match the Core's `JWT_SECRET`), and the optional `NEXT_PUBLIC_*` analytics keys. `CLERK_*`, `STRIPE_*`, `AWS_*`, `SENTRY_*` (build-time), `LOOPS_*`, and any SST-injected var are absent from both files. `LOOPS_API_KEY` is removed from `apps/website/.env.example`."
    ],
    "acceptance_checks": [
      "AC-045"
    ],
    "depends_on": [
      "AC-044"
    ],
    "verify_first": false,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 1,
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-046",
    "context": "open-source-local-runtime",
    "category": "security",
    "description": "`apps/dashboard/src/middleware.ts` no longer imports `clerkMiddleware`. The middleware reads the `__session` httpOnly cookie (set by the dashboard's sign-in route handler), verifies it with `JWT_SECRET` using the same `jose` library the Core uses, and lets the request through. `withAuth(handler, options)` extracts the cookie, calls the Core's `GET /api/v1/whoami` with `Authorization: Bearer `, and resolves `userId` + `tenantId` + `role` from the response. The dashboard's `/auth/sign-in` and `/auth/sign-up` pages are local React forms that POST to a new dashboard route handler (`/api/auth/login`, `/api/auth/register`) which proxies to the Core's endpoints, then sets the `__session` cookie. An unauthenticated `GET /dashboard` still redirects to `/auth/sign-in` (preserving AC-019's behavior).",
    "steps": [
      "Verify AC-046: `apps/dashboard/src/middleware.ts` no longer imports `clerkMiddleware`. The middleware reads the `__session` httpOnly cookie (set by the dashboard's sign-in route handler), verifies it with `JWT_SECRET` using the same `jose` library the Core uses, and lets the request through. `withAuth(handler, options)` extracts the cookie, calls the Core's `GET /api/v1/whoami` with `Authorization: Bearer `, and resolves `userId` + `tenantId` + `role` from the response. The dashboard's `/auth/sign-in` and `/auth/sign-up` pages are local React forms that POST to a new dashboard route handler (`/api/auth/login`, `/api/auth/register`) which proxies to the Core's endpoints, then sets the `__session` cookie. An unauthenticated `GET /dashboard` still redirects to `/auth/sign-in` (preserving AC-019's behavior)."
    ],
    "acceptance_checks": [
      "AC-046"
    ],
    "depends_on": [
      "AC-044"
    ],
    "verify_first": false,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-047",
    "context": "open-source-local-runtime",
    "category": "foundation",
    "description": "`@clerk/nextjs` (and any `@clerk/*` subpackage) is removed from `apps/dashboard/package.json#dependencies`. `pnpm install --frozen-lockfile` succeeds. `pnpm --filter dashboard build` (which runs `tsc --noEmit --project tsconfig.build.json` + `next build`) exits 0. `grep -r '@clerk' apps/dashboard/src apps/dashboard/scripts` returns zero matches. The `tsconfig.build.json` (which previously excluded Clerk type tests) is simplified to a standard dashboard build config; if any Clerk-flavored type-test files remain, they are deleted.",
    "steps": [
      "Verify AC-047: `@clerk/nextjs` (and any `@clerk/*` subpackage) is removed from `apps/dashboard/package.json#dependencies`. `pnpm install --frozen-lockfile` succeeds. `pnpm --filter dashboard build` (which runs `tsc --noEmit --project tsconfig.build.json` + `next build`) exits 0. `grep -r '@clerk' apps/dashboard/src apps/dashboard/scripts` returns zero matches. The `tsconfig.build.json` (which previously excluded Clerk type tests) is simplified to a standard dashboard build config; if any Clerk-flavored type-test files remain, they are deleted."
    ],
    "acceptance_checks": [
      "AC-047"
    ],
    "depends_on": [
      "AC-046"
    ],
    "verify_first": false,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-048",
    "context": "open-source-local-runtime",
    "category": "security",
    "description": "`@stripe/react-stripe-js`, `@stripe/stripe-js`, and the `stripe` server SDK are removed from `apps/dashboard/package.json`. `apps/dashboard/src/contexts/billing/presentation/components/payment-modal.tsx` no longer mounts a Stripe `PaymentElement`; it renders a local \"Billing disabled in OSS build\" panel and POSTs to the Core's `POST /api/v1/billing/checkout-session`, which returns 410 Gone per the Core's AC-043. The plan-gate (`apps/dashboard/src/contexts/billing/application/plan-status.ts#fetchPlanStatus`) renders correctly when the Core returns `{ plan: 'free', status: 'active' }` (a fresh tenant on the OSS build). The dashboard's `POST /api/billing/checkout` and `POST /api/billing/portal` route handlers stay (they proxy to the Core) and surface the 410 from the Core as a clear \"billing disabled\" message.",
    "steps": [
      "Verify AC-048: `@stripe/react-stripe-js`, `@stripe/stripe-js`, and the `stripe` server SDK are removed from `apps/dashboard/package.json`. `apps/dashboard/src/contexts/billing/presentation/components/payment-modal.tsx` no longer mounts a Stripe `PaymentElement`; it renders a local \"Billing disabled in OSS build\" panel and POSTs to the Core's `POST /api/v1/billing/checkout-session`, which returns 410 Gone per the Core's AC-043. The plan-gate (`apps/dashboard/src/contexts/billing/application/plan-status.ts#fetchPlanStatus`) renders correctly when the Core returns `{ plan: 'free', status: 'active' }` (a fresh tenant on the OSS build). The dashboard's `POST /api/billing/checkout` and `POST /api/billing/portal` route handlers stay (they proxy to the Core) and surface the 410 from the Core as a clear \"billing disabled\" message."
    ],
    "acceptance_checks": [
      "AC-048"
    ],
    "depends_on": [
      "AC-046"
    ],
    "verify_first": false,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-049",
    "context": "open-source-local-runtime",
    "category": "foundation",
    "description": "`@aws-sdk/client-dynamodb`, `@aws-sdk/lib-dynamodb`, and `@aws-sdk/client-cognito-identity-provider` are removed from `apps/dashboard/package.json` (dependencies) and from the root `package.json` (if present). `apps/dashboard/src/contexts/identity/infrastructure/tenant-provisioning-fallback.ts` is deleted (the OSS Core handles provisioning inside its own `register` endpoint per the Core's AC-042). `apps/dashboard/scripts/setup-stripe.ts` and `apps/dashboard/scripts/delete-user.ts` are deleted. The `serverExternalPackages: ['@aws-sdk/client-cognito-identity-provider']` block in `apps/dashboard/next.config.mjs` is removed. `apps/dashboard/scripts/add-credits.ts` keeps its Core API call and is the only remaining script. `pnpm --filter dashboard build` exits 0.",
    "steps": [
      "Verify AC-049: `@aws-sdk/client-dynamodb`, `@aws-sdk/lib-dynamodb`, and `@aws-sdk/client-cognito-identity-provider` are removed from `apps/dashboard/package.json` (dependencies) and from the root `package.json` (if present). `apps/dashboard/src/contexts/identity/infrastructure/tenant-provisioning-fallback.ts` is deleted (the OSS Core handles provisioning inside its own `register` endpoint per the Core's AC-042). `apps/dashboard/scripts/setup-stripe.ts` and `apps/dashboard/scripts/delete-user.ts` are deleted. The `serverExternalPackages: ['@aws-sdk/client-cognito-identity-provider']` block in `apps/dashboard/next.config.mjs` is removed. `apps/dashboard/scripts/add-credits.ts` keeps its Core API call and is the only remaining script. `pnpm --filter dashboard build` exits 0."
    ],
    "acceptance_checks": [
      "AC-049"
    ],
    "depends_on": [
      "AC-044"
    ],
    "verify_first": false,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-050",
    "context": "open-source-local-runtime",
    "category": "ops",
    "description": "`apps/website/sst.config.ts` and `apps/dashboard/sst.config.ts` are deleted. The `withSentryConfig(...)` wrapper in `apps/dashboard/next.config.mjs` is removed (the surrounding `next.config.mjs` keeps its `optimizePackageImports`, headers, redirects, and image allow-list). `.github/workflows/website-deploy.yml` and `.github/workflows/dashboard-deploy.yml` keep their `check-types` / `lint` / `test` / `build` steps but drop the `sst deploy` step (replaced with a comment pointing operators to the Docker build). Both apps have a multi-stage `Dockerfile` that mirrors the `relay` subproject's pattern: a builder stage that runs `pnpm install --frozen-lockfile` + `pnpm build`, then a runtime stage that runs `pnpm start`. The `docker-compose.yml` from AC-044 references those Dockerfiles.",
    "steps": [
      "Verify AC-050: `apps/website/sst.config.ts` and `apps/dashboard/sst.config.ts` are deleted. The `withSentryConfig(...)` wrapper in `apps/dashboard/next.config.mjs` is removed (the surrounding `next.config.mjs` keeps its `optimizePackageImports`, headers, redirects, and image allow-list). `.github/workflows/website-deploy.yml` and `.github/workflows/dashboard-deploy.yml` keep their `check-types` / `lint` / `test` / `build` steps but drop the `sst deploy` step (replaced with a comment pointing operators to the Docker build). Both apps have a multi-stage `Dockerfile` that mirrors the `relay` subproject's pattern: a builder stage that runs `pnpm install --frozen-lockfile` + `pnpm build`, then a runtime stage that runs `pnpm start`. The `docker-compose.yml` from AC-044 references those Dockerfiles."
    ],
    "acceptance_checks": [
      "AC-050"
    ],
    "depends_on": [
      "AC-044"
    ],
    "verify_first": false,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "observation_method": "grep",
    "observation_methods": [
      "grep"
    ]
  },
  {
    "id": "WI-AC-051",
    "context": "open-source-local-runtime",
    "category": "foundation",
    "description": "`logos.composio.dev` and `backend.composio.dev` are removed from `next.config.mjs#images.remotePatterns` (and any CSP allow-list). The `composioTriggerId` field is removed from the `Integration` domain type at `apps/dashboard/src/contexts/integrations/domain/types.ts`; the 15 integration type identifiers (Slack, GitHub, Jira, AWS CloudWatch, HubSpot, Trello, PostgreSQL, Linear, Sentry, MongoDB, Datadog, PagerDuty, Grafana, Confluence, Webhooks) stay. The `/dashboard/integrations` page still renders 15 connection cards; the \"Connect\" CTA POSTs to the Core's stub integration endpoint, which returns 200 with deterministic empty data. No `COMPOSIO_API_KEY` env var is referenced anywhere in the OSS build.",
    "steps": [
      "Verify AC-051: `logos.composio.dev` and `backend.composio.dev` are removed from `next.config.mjs#images.remotePatterns` (and any CSP allow-list). The `composioTriggerId` field is removed from the `Integration` domain type at `apps/dashboard/src/contexts/integrations/domain/types.ts`; the 15 integration type identifiers (Slack, GitHub, Jira, AWS CloudWatch, HubSpot, Trello, PostgreSQL, Linear, Sentry, MongoDB, Datadog, PagerDuty, Grafana, Confluence, Webhooks) stay. The `/dashboard/integrations` page still renders 15 connection cards; the \"Connect\" CTA POSTs to the Core's stub integration endpoint, which returns 200 with deterministic empty data. No `COMPOSIO_API_KEY` env var is referenced anywhere in the OSS build."
    ],
    "acceptance_checks": [
      "AC-051"
    ],
    "depends_on": [
      "AC-044"
    ],
    "verify_first": false,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-052",
    "context": "open-source-local-runtime",
    "category": "foundation",
    "description": "The `connect-src` CSP entry for `https://app.loops.so` is removed from `apps/website/next.config.mjs`. `LOOPS_API_KEY` is removed from `apps/website/.env.example`. The `lvh.me` → `localhost` rewrite in `apps/dashboard/src/middleware.ts` and the inverse `localhost` → `lvh.me` rewrite in the dashboard's checkout/portal API handlers are removed (no Stripe callbacks to rewrite in the OSS build). The dashboard's `curl http://localhost:3001/...` requests work as-is; no `lvh.me` host appears anywhere in the dashboard's source.",
    "steps": [
      "Verify AC-052: The `connect-src` CSP entry for `https://app.loops.so` is removed from `apps/website/next.config.mjs`. `LOOPS_API_KEY` is removed from `apps/website/.env.example`. The `lvh.me` → `localhost` rewrite in `apps/dashboard/src/middleware.ts` and the inverse `localhost` → `lvh.me` rewrite in the dashboard's checkout/portal API handlers are removed (no Stripe callbacks to rewrite in the OSS build). The dashboard's `curl http://localhost:3001/...` requests work as-is; no `lvh.me` host appears anywhere in the dashboard's source."
    ],
    "acceptance_checks": [
      "AC-052"
    ],
    "depends_on": [
      "AC-044"
    ],
    "verify_first": false,
    "implementation": true,
    "qa": true,
    "integration": true,
    "retries": 0,
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-053",
    "context": "open-source-local-runtime",
    "category": "foundation",
    "description": "With the OSS docker-compose stack up from AC-044, the following original checks still pass (any check that described a vendor-specific implementation now passes against the local equivalent — e.g. AC-019's redirect behavior is preserved by the local JWT middleware, AC-022's 402 CREDITS_EXHAUSTED is preserved by the local credits ledger, AC-031's \"no STRIPE_SECRET_KEY in dashboard env\" is trivially true because Stripe is gone, AC-037's \"AWS SDK limited to two files\" is trivially true because AWS SDK is gone). Concretely: `pnpm turbo check-types` + `pnpm turbo lint` + `pnpm turbo test` + `pnpm turbo build` all exit 0 inside the dashboard container; `curl http://localhost:3000/` returns 200; the dashboard's sign-in form works end-to-end with the Core's local auth; `GET /api/incidents` (with a valid cookie) returns the Core's stub data; the plan-gate renders correctly for a fresh tenant; Sentry is a no-op with `NEXT_PUBLIC_SENTRY_DSN` empty.",
    "steps": [
      "Verify AC-053: With the OSS docker-compose stack up from AC-044, the following original checks still pass (any check that described a vendor-specific implementation now passes against the local equivalent — e.g. AC-019's redirect behavior is preserved by the local JWT middleware, AC-022's 402 CREDITS_EXHAUSTED is preserved by the local credits ledger, AC-031's \"no STRIPE_SECRET_KEY in dashboard env\" is trivially true because Stripe is gone, AC-037's \"AWS SDK limited to two files\" is trivially true because AWS SDK is gone). Concretely: `pnpm turbo check-types` + `pnpm turbo lint` + `pnpm turbo test` + `pnpm turbo build` all exit 0 inside the dashboard container; `curl http://localhost:3000/` returns 200; the dashboard's sign-in form works end-to-end with the Core's local auth; `GET /api/incidents` (with a valid cookie) returns the Core's stub data; the plan-gate renders correctly for a fresh tenant; Sentry is a no-op with `NEXT_PUBLIC_SENTRY_DSN` empty."
    ],
    "acceptance_checks": [
      "AC-053"
    ],
    "depends_on": [
      "AC-044",
      "AC-046",
      "AC-048",
      "AC-049"
    ],
    "verify_first": false,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0,
    "observation_method": "http",
    "observation_methods": [
      "http"
    ]
  },
  {
    "id": "WI-AC-054",
    "context": "open-source-local-runtime",
    "category": "testing",
    "observation_method": "browser",
    "observation_methods": [
      "browser"
    ],
    "description": "A dedicated Playwright project (e.g. `dashboard-oss-e2e`) targets the OSS compose stack (dashboard on the documented compose port, `CORE_API_URL` pointing at Core `:3099`). Auth setup uses Core local register/login + session cookie only — zero Clerk, zero `.env.staging` STAGING_TEST_USER path, zero `page.route` mocks of `/api/integrations/*` or `/api/incidents*` as the pass path. `pnpm exec playwright test --project=dashboard-oss-e2e` is the command operators and harness QA run for AC-055..AC-057.",
    "steps": [
      "Verify AC-054: A dedicated Playwright project (e.g. `dashboard-oss-e2e`) targets the OSS compose stack (dashboard on the documented compose port, `CORE_API_URL` pointing at Core `:3099`). Auth setup uses Core local register/login + session cookie only — zero Clerk, zero `.env.staging` STAGING_TEST_USER path, zero `page.route` mocks of `/api/integrations/*` or `/api/incidents*` as the pass path. `pnpm exec playwright test --project=dashboard-oss-e2e` is the command operators and harness QA run for AC-055..AC-057."
    ],
    "acceptance_checks": [
      "AC-054"
    ],
    "planning_decision_ids": [
      "PD-OSS-DASHBOARD-E2E-AUTH"
    ],
    "depends_on": [
      "AC-044",
      "AC-046",
      "AC-051"
    ],
    "verify_first": false,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0
  },
  {
    "id": "WI-AC-055",
    "context": "open-source-local-runtime",
    "category": "functional",
    "observation_method": "browser",
    "observation_methods": [
      "browser"
    ],
    "description": "Signed in via AC-054, the operator opens `/dashboard/integrations`, connects at least one integration through the Core OSS/stub integration API (Composio remains removed). The connect flow reaches a Core-owned mock/stub upstream application or webhook simulator (not a Playwright `page.route` fake of the dashboard BFF). After connect, the integrations UI shows the connector as connected/ready and a subsequent Core API read confirms the stub connection record exists for the tenant.",
    "steps": [
      "Verify AC-055: Signed in via AC-054, the operator opens `/dashboard/integrations`, connects at least one integration through the Core OSS/stub integration API (Composio remains removed). The connect flow reaches a Core-owned mock/stub upstream application or webhook simulator (not a Playwright `page.route` fake of the dashboard BFF). After connect, the integrations UI shows the connector as connected/ready and a subsequent Core API read confirms the stub connection record exists for the tenant."
    ],
    "acceptance_checks": [
      "AC-055"
    ],
    "planning_decision_ids": [
      "PD-OSS-DASHBOARD-E2E-CONNECTORS"
    ],
    "depends_on": [
      "AC-054",
      "AC-051"
    ],
    "verify_first": false,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0
  },
  {
    "id": "WI-AC-056",
    "context": "open-source-local-runtime",
    "category": "functional",
    "observation_method": "browser",
    "observation_methods": [
      "browser"
    ],
    "description": "From the OSS dashboard (local auth), create a real incident/analysis (UI or authenticated `POST` through the dashboard BFF into Core). Core persists the incident in Postgres. The dashboard incident/analysis detail page opens a live SSE (or equivalent) feed and shows triage/investigation progress from Core — not fixture JSON injected by Playwright. Credits/ledger rules from AC-022 still apply on the OSS stack.",
    "steps": [
      "Verify AC-056: From the OSS dashboard (local auth), create a real incident/analysis (UI or authenticated `POST` through the dashboard BFF into Core). Core persists the incident in Postgres. The dashboard incident/analysis detail page opens a live SSE (or equivalent) feed and shows triage/investigation progress from Core — not fixture JSON injected by Playwright. Credits/ledger rules from AC-022 still apply on the OSS stack."
    ],
    "acceptance_checks": [
      "AC-056"
    ],
    "planning_decision_ids": [
      "PD-OSS-DASHBOARD-E2E-INCIDENT"
    ],
    "depends_on": [
      "AC-054",
      "AC-055"
    ],
    "verify_first": false,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0
  },
  {
    "id": "WI-AC-057",
    "context": "open-source-local-runtime",
    "category": "functional",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ],
    "description": "With Ornith 9B serving on `http://127.0.0.1:8081` (`~/tools/llama-session.sh`), the AC-056 incident's triage/investigation happy path uses Core's default OpenAI-compatible LLM connector pointed at Ornith (model alias `Ornith-1.0-9B-code`). Evidence must show at least one successful completion call to the local connector during the run (Core LLM audit/metrics, llama-server access log, or documented `/admin` trace) — Anthropic must not be required and a silent DeterministicLLMClient stub must not be the pass path for this check. If Ornith is stopped, the same E2E fails closed with a clear Core health/investigation error instead of hanging or auto-passing via stub.",
    "steps": [
      "Verify AC-057: With Ornith 9B serving on `http://127.0.0.1:8081` (`~/tools/llama-session.sh`), the AC-056 incident's triage/investigation happy path uses Core's default OpenAI-compatible LLM connector pointed at Ornith (model alias `Ornith-1.0-9B-code`). Evidence must show at least one successful completion call to the local connector during the run (Core LLM audit/metrics, llama-server access log, or documented `/admin` trace) — Anthropic must not be required and a silent DeterministicLLMClient stub must not be the pass path for this check. If Ornith is stopped, the same E2E fails closed with a clear Core health/investigation error instead of hanging or auto-passing via stub."
    ],
    "acceptance_checks": [
      "AC-057"
    ],
    "planning_decision_ids": [
      "PD-OSS-DASHBOARD-E2E-ORNITH"
    ],
    "depends_on": [
      "AC-056"
    ],
    "verify_first": false,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0
  },
  {
    "id": "WI-AC-058",
    "context": "open-source-local-runtime",
    "category": "functional",
    "observation_method": "browser",
    "observation_methods": [
      "browser"
    ],
    "description": "From the OSS dashboard (local auth), the user connects a Core-owned **runnable test application** (not a Playwright `page.route` fake) via `/dashboard/integrations`. At least two integration types (or one app plus one additional connector) are enabled against that test app through Core OSS/stub endpoints. The UI shows connected/enabled state and Core persists the connection records for the tenant. Composio remains unused.",
    "steps": [
      "Verify AC-058: From the OSS dashboard (local auth), the user connects a Core-owned **runnable test application** (not a Playwright `page.route` fake) via `/dashboard/integrations`. At least two integration types (or one app plus one additional connector) are enabled against that test app through Core OSS/stub endpoints. The UI shows connected/enabled state and Core persists the connection records for the tenant. Composio remains unused."
    ],
    "acceptance_checks": [
      "AC-058"
    ],
    "planning_decision_ids": [
      "PD-OSS-DASHBOARD-GOLDEN-PATH",
      "PD-OSS-TEST-APP"
    ],
    "depends_on": [
      "AC-054",
      "AC-055"
    ],
    "verify_first": false,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0
  },
  {
    "id": "WI-AC-059",
    "context": "open-source-local-runtime",
    "category": "functional",
    "observation_method": "http",
    "observation_methods": [
      "http"
    ],
    "description": "The OSS dashboard (or settings surface it proxies) can select Core's investigation LLM connector. Default is Ornith 9B on `:8081`. When Ornith's context window is insufficient for the investigation (documented overflow / context-too-large signal from Core), the operator switches to **DeepSeek V4 Flash** via OpenCode Go (`opencode/deepseek-v4-flash-free`) or NVIDIA NIM (`deepseek-ai/deepseek-v4-pro` or `deepseek-v4-flash`). Credentials are supplied at runtime from the operator host (`~/.config/opencode`, `~/.pi/agent/auth.json`, or env vars derived from them) and are never committed to the repo. After selection, Core health/settings reflect the active connector.",
    "steps": [
      "Verify AC-059: The OSS dashboard (or settings surface it proxies) can select Core's investigation LLM connector. Default is Ornith 9B on `:8081`. When Ornith's context window is insufficient for the investigation (documented overflow / context-too-large signal from Core), the operator switches to **DeepSeek V4 Flash** via OpenCode Go (`opencode/deepseek-v4-flash-free`) or NVIDIA NIM (`deepseek-ai/deepseek-v4-pro` or `deepseek-v4-flash`). Credentials are supplied at runtime from the operator host (`~/.config/opencode`, `~/.pi/agent/auth.json`, or env vars derived from them) and are never committed to the repo. After selection, Core health/settings reflect the active connector."
    ],
    "acceptance_checks": [
      "AC-059"
    ],
    "planning_decision_ids": [
      "PD-OSS-DASHBOARD-GOLDEN-PATH",
      "PD-OSS-LLM-FALLBACK"
    ],
    "depends_on": [
      "AC-054",
      "AC-057"
    ],
    "verify_first": false,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0
  },
  {
    "id": "WI-AC-060",
    "context": "open-source-local-runtime",
    "category": "functional",
    "observation_method": "browser",
    "observation_methods": [
      "browser"
    ],
    "description": "Using the connected test application (AC-058) and configured LLM (AC-059), the dashboard drives a full investigation on a real Core incident: analyses list/detail update, triage assigns severity, the user can **talk to the model on the incident** (chat panel returns model-backed replies), the UI shows a **root-cause** hypothesis with evidence, and Core produces a **remediation** proposal visible in the dashboard. All progress is live from Core (SSE or documented polling), not Playwright fixtures. DeterministicLLMClient is not the pass path.",
    "steps": [
      "Verify AC-060: Using the connected test application (AC-058) and configured LLM (AC-059), the dashboard drives a full investigation on a real Core incident: analyses list/detail update, triage assigns severity, the user can **talk to the model on the incident** (chat panel returns model-backed replies), the UI shows a **root-cause** hypothesis with evidence, and Core produces a **remediation** proposal visible in the dashboard. All progress is live from Core (SSE or documented polling), not Playwright fixtures. DeterministicLLMClient is not the pass path."
    ],
    "acceptance_checks": [
      "AC-060"
    ],
    "planning_decision_ids": [
      "PD-OSS-DASHBOARD-GOLDEN-PATH",
      "PD-OSS-TEST-APP"
    ],
    "depends_on": [
      "AC-056",
      "AC-058",
      "AC-059"
    ],
    "verify_first": false,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0
  },
  {
    "id": "WI-AC-061",
    "context": "open-source-local-runtime",
    "category": "testing",
    "observation_method": "browser",
    "observation_methods": [
      "browser"
    ],
    "description": "`pnpm exec playwright test --project=dashboard-oss-e2e` (or the documented suite name) runs one end-to-end scenario that chains AC-054..AC-060: local auth → connect test app → enable integrations → configure Ornith/DeepSeek → create incident → analyses/triage/chat/root-cause/remediation. The suite exits 0 only when every stage passes against compose Core. This suite is required evidence for Goal Review / `run_completed` on web — partial AC-055/057 smoke alone does not satisfy open-source delivery.",
    "steps": [
      "Verify AC-061: `pnpm exec playwright test --project=dashboard-oss-e2e` (or the documented suite name) runs one end-to-end scenario that chains AC-054..AC-060: local auth → connect test app → enable integrations → configure Ornith/DeepSeek → create incident → analyses/triage/chat/root-cause/remediation. The suite exits 0 only when every stage passes against compose Core. This suite is required evidence for Goal Review / `run_completed` on web — partial AC-055/057 smoke alone does not satisfy open-source delivery."
    ],
    "acceptance_checks": [
      "AC-061"
    ],
    "planning_decision_ids": [
      "PD-OSS-DASHBOARD-GOLDEN-PATH",
      "PD-OSS-LLM-FALLBACK",
      "PD-OSS-TEST-APP"
    ],
    "depends_on": [
      "AC-054",
      "AC-058",
      "AC-059",
      "AC-060"
    ],
    "verify_first": false,
    "implementation": false,
    "qa": false,
    "integration": false,
    "retries": 0
  }
]
